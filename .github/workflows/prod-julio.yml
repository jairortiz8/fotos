name: prod-julio (one-off)
on:
  workflow_dispatch:
  push:
    branches: ["run-julio"]
concurrency:
  group: prod-julio
  cancel-in-progress: true
jobs:
  avatares:
    name: Regenerar avatares con el umbral nuevo
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      API: https://backboard.railway.app/graphql/v2
      PROJECT_ID: 13f846c9-6400-44dc-995f-02a7da9c45b4
      ENV_ID: b48c37f0-dab0-4627-b95c-a20db2862579
      PG_ID: 17602daf-d2a8-4b4e-99b9-738d2583e624
      WORKER_ID: 1846c52a-57c3-4a8b-b4ec-1cc200a8e652
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install --quiet "psycopg[binary]" requests redis celery

      - name: Credenciales
        run: |
          python3 - <<'PYX'
          import os, requests
          API=os.environ["API"]; TOK=os.environ["RAILWAY_TOKEN"]
          H={"Authorization":f"Bearer {TOK}","Project-Access-Token":TOK,"Content-Type":"application/json"}
          q='query($p:String!,$e:String!,$s:String!){ variables(projectId:$p,environmentId:$e,serviceId:$s) }'
          def vf(sid):
              v={"p":os.environ["PROJECT_ID"],"e":os.environ["ENV_ID"],"s":sid}
              return (requests.post(API,headers=H,json={"query":q,"variables":v},timeout=40)
                      .json().get("data",{}) or {}).get("variables") or {}
          pg=vf(os.environ["PG_ID"])
          q1='query($e:String!){ environment(id:$e){ serviceInstances{ edges{ node{ serviceId serviceName } } } } }'
          d=requests.post(API,headers=H,json={"query":q1,"variables":{"e":os.environ["ENV_ID"]}},timeout=40).json()
          rid=next((e["node"]["serviceId"] for e in d["data"]["environment"]["serviceInstances"]["edges"]
                    if e["node"]["serviceName"].lower().startswith("redis")), None)
          out={"DATABASE_URL":pg.get("DATABASE_PUBLIC_URL"),
               "REDIS_PUB":vf(rid).get("REDIS_PUBLIC_URL") if rid else None}
          if [k for k,v in out.items() if not v]: raise SystemExit("faltan credenciales")
          with open(os.environ["GITHUB_ENV"],"a") as f:
              for k,v in out.items():
                  print(f"::add-mask::{v}"); f.write(f"{k}={v}\n")
          PYX

      - name: 1 - El worker ya tiene el arreglo desplegado?
        run: |
          python3 - <<'PYX'
          import os, json, urllib.request
          API=os.environ["API"]; TOK=os.environ["RAILWAY_TOKEN"]
          def gql(q,v=None):
              body=json.dumps({"query":q,"variables":v or {}}).encode()
              req=urllib.request.Request(API,data=body,headers={
                  "Authorization":f"Bearer {TOK}","Project-Access-Token":TOK,
                  "Content-Type":"application/json","User-Agent":"curl/8.0"})
              return json.load(urllib.request.urlopen(req,timeout=60))
          q="""query($e:String!){ environment(id:$e){ serviceInstances{ edges{ node{
                serviceName latestDeployment{ status meta } } } } } }"""
          d=gql(q,{"e":os.environ["ENV_ID"]})
          listo="no"
          for e in d["data"]["environment"]["serviceInstances"]["edges"]:
              n=e["node"]; dep=n.get("latestDeployment") or {}
              meta=dep.get("meta") or {}
              msg=((meta.get("commitMessage") or "").splitlines() or [""])[0][:52]
              sha=(meta.get("commitHash") or "")[:7]
              print(f"  {n['serviceName']:<12} {dep.get('status') or 'SIN DEPLOY':<12} {sha} {msg}")
              if n["serviceName"]=="worker" and "visor" in msg.lower() and dep.get("status")=="SUCCESS":
                  listo="si"
          print(f"\n  worker con el arreglo del visor: {listo}")
          with open(os.environ["GITHUB_ENV"],"a") as f: f.write(f"DESPLEGADO={listo}\n")
          PYX

      - name: 2 - Estado de las fotos (por que 'listas' bajo a 0)
        run: |
          python3 - <<'PYX'
          import os, psycopg
          SLUG="garmin-runners-girls-5k-social-run"
          with psycopg.connect(os.environ["DATABASE_URL"]) as conn, conn.cursor() as cur:
              cur.execute("SELECT id FROM events_event WHERE slug=%s",(SLUG,))
              eid=cur.fetchone()[0]
              with open(os.environ["GITHUB_ENV"],"a") as f: f.write(f"EVENT_ID={eid}\n")
              cur.execute("""SELECT status, count(*) FROM photos_photo
                              WHERE event_id=%s GROUP BY 1 ORDER BY 2 DESC""",(eid,))
              print("=== FOTOS POR ESTADO ===")
              for st,n in cur.fetchall(): print(f"  {st:<20} {n}")
              cur.execute("""SELECT count(*) FROM photos_bib b
                              JOIN photos_photo p ON p.id=b.photo_id WHERE p.event_id=%s""",(eid,))
              print(f"\n  dorsales leidos: {cur.fetchone()[0]}")
          PYX

      - name: 3 - Re-encolar los avatares
        if: env.DESPLEGADO == 'si'
        run: |
          python3 - <<'PYX'
          import os, psycopg
          from celery import Celery
          eid=int(os.environ["EVENT_ID"])
          with psycopg.connect(os.environ["DATABASE_URL"]) as conn, conn.cursor() as cur:
              # Sólo fotos que tienen caras sin avatar: es lo único que el
              # umbral nuevo puede cambiar. La task saltea las ya hechas.
              cur.execute("""SELECT DISTINCT fe.photo_id
                               FROM photos_faceembedding fe
                               JOIN photos_photo p ON p.id=fe.photo_id
                              WHERE p.event_id=%s AND fe.avatar_key=''
                                AND p.original_key<>''
                              ORDER BY 1""",(eid,))
              ids=[r[0] for r in cur.fetchall()]
          print(f"  fotos con caras sin avatar: {len(ids)}")
          if not ids:
              print("  nada para re-encolar"); raise SystemExit(0)
          app=Celery(broker=os.environ["REDIS_PUB"])
          for pid in ids:
              app.send_task("photos.generate_face_avatars", args=[pid])
          print(f"  re-encoladas {len(ids)} tareas de avatares (cola 'celery')")
          PYX

      - name: 3b - No desplegado todavia
        if: env.DESPLEGADO != 'si'
        run: |
          echo "El worker todavia no corre el arreglo del visor."
          echo "No re-encolo avatares: se generarian con el umbral VIEJO."
          echo "Hay que repetir este paso cuando el deploy termine."
