name: prod-julio (one-off)
on:
  workflow_dispatch:
  push:
    branches: ["run-julio"]
concurrency:
  group: prod-julio
  cancel-in-progress: true
jobs:
  arreglo:
    name: ARREGLO - que el worker consuma la cola fast
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      API: https://backboard.railway.app/graphql/v2
      PROJECT_ID: 13f846c9-6400-44dc-995f-02a7da9c45b4
      ENV_ID: b48c37f0-dab0-4627-b95c-a20db2862579
      PG_ID: 17602daf-d2a8-4b4e-99b9-738d2583e624
      WEB_ID: 5dec515d-7408-46ae-91ac-d2325d6d92ad
      WORKER_ID: 1846c52a-57c3-4a8b-b4ec-1cc200a8e652
      DJANGO_SETTINGS_MODULE: config.settings.dev
      SITE_NAME: "find your foto"
      SITE_DOMAIN: findyourfoto.com
      NOTIFIER_BACKEND: whatsapp_manual
      LANGUAGE_CODE: es
      TIME_ZONE: America/El_Salvador
      FACE_SEARCH_ENABLED: "false"
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install --quiet "psycopg[binary]" requests redis

      - name: Credenciales de prod
        run: |
          python3 - <<'PYX'
          import os, requests
          API=os.environ["API"]; TOK=os.environ["RAILWAY_TOKEN"]
          H={"Authorization":f"Bearer {TOK}","Project-Access-Token":TOK,"Content-Type":"application/json"}
          q='query($p:String!,$e:String!,$s:String!){ variables(projectId:$p,environmentId:$e,serviceId:$s) }'
          def vf(sid):
              v={"p":os.environ["PROJECT_ID"],"e":os.environ["ENV_ID"],"s":sid}
              return (requests.post(API,headers=H,json={"query":q,"variables":v},timeout=40)
                      .json().get("data",{}) or {}).get("variables") or {}
          pg=vf(os.environ["PG_ID"])
          q1='query($e:String!){ environment(id:$e){ serviceInstances{ edges{ node{ serviceId serviceName } } } } }'
          d=requests.post(API,headers=H,json={"query":q1,"variables":{"e":os.environ["ENV_ID"]}},timeout=40).json()
          rid=next((e["node"]["serviceId"] for e in d["data"]["environment"]["serviceInstances"]["edges"]
                    if e["node"]["serviceName"].lower().startswith("redis")), None)
          pub=vf(rid).get("REDIS_PUBLIC_URL") if rid else None
          out={"DATABASE_URL":pg.get("DATABASE_PUBLIC_URL"), "REDIS_PUB":pub}
          faltan=[k for k,v in out.items() if not v]
          if faltan: raise SystemExit(f"faltan: {faltan}")
          with open(os.environ["GITHUB_ENV"],"a") as f:
              for k,v in out.items():
                  print(f"::add-mask::{v}"); f.write(f"{k}={v}\n")
          print("  credenciales listas")
          PYX

      - name: 1 - Confirmar el diagnostico
        run: |
          python3 - <<'PYX'
          import os, requests, redis
          API=os.environ["API"]; TOK=os.environ["RAILWAY_TOKEN"]
          H={"Authorization":f"Bearer {TOK}","Project-Access-Token":TOK,"Content-Type":"application/json"}
          q='query($p:String!,$e:String!,$s:String!){ variables(projectId:$p,environmentId:$e,serviceId:$s) }'
          v={"p":os.environ["PROJECT_ID"],"e":os.environ["ENV_ID"],"s":os.environ["WORKER_ID"]}
          wk=(requests.post(API,headers=H,json={"query":q,"variables":v},timeout=40)
              .json().get("data",{}) or {}).get("variables") or {}
          print(f"  worker.WORKER_QUEUES (antes) = {wk.get('WORKER_QUEUES','(sin setear)')}")
          r=redis.from_url(os.environ["REDIS_PUB"], socket_timeout=15, socket_connect_timeout=15)
          print("\n=== TAREAS ESPERANDO POR COLA ===")
          for cola in ("fast","faces","celery"):
              print(f"  {cola:<8} {r.llen(cola)} tareas")
          print("\n  'fast' con tareas + WORKER_QUEUES sin 'fast' = nadie las consume")
          PYX

      - name: 2 - Aplicar el arreglo
        run: |
          python3 - <<'PYX'
          import os, json, urllib.request
          API=os.environ["API"]; TOK=os.environ["RAILWAY_TOKEN"]
          def gql(q,v=None):
              body=json.dumps({"query":q,"variables":v or {}}).encode()
              req=urllib.request.Request(API,data=body,headers={
                  "Authorization":f"Bearer {TOK}","Project-Access-Token":TOK,
                  "Content-Type":"application/json","User-Agent":"curl/8.0"})
              return json.load(urllib.request.urlopen(req,timeout=60))
          # Prioridad esta noche: que salgan los previews. El worker se dedica a
          # `fast` y `celery`; las caras (~15s c/u) quedan encoladas y se ponen al
          # dia despues volviendo a `celery,faces,fast`.
          M="""mutation($p:String!,$e:String!,$s:String!,$n:String!,$v:String!){
                 variableUpsert(input:{projectId:$p, environmentId:$e, serviceId:$s, name:$n, value:$v}) }"""
          r=gql(M,{"p":os.environ["PROJECT_ID"],"e":os.environ["ENV_ID"],
                   "s":os.environ["WORKER_ID"],"n":"WORKER_QUEUES","v":"fast,celery"})
          print(f"  WORKER_QUEUES = fast,celery -> {json.dumps(r.get('data'))}")
          if r.get("errors"): raise SystemExit(f"ERROR: {r['errors']}")
          D="""mutation($s:String!,$e:String!){ serviceInstanceDeployV2(serviceId:$s, environmentId:$e) }"""
          r2=gql(D,{"s":os.environ["WORKER_ID"],"e":os.environ["ENV_ID"]})
          print(f"  redeploy disparado -> {json.dumps(r2.get('data'))[:120]}")
          if r2.get("errors"): print(f"  ERRORES: {r2['errors']}")
          PYX

      - name: 3 - Esperar el arranque
        run: sleep 270

      - name: 4 - Verificar que esta procesando
        if: always()
        run: |
          python3 - <<'PYX'
          import os, psycopg, redis, time
          SLUG="garmin-runners-girls-5k-social-run"
          r=redis.from_url(os.environ["REDIS_PUB"], socket_timeout=15)

          def foto_estado():
              with psycopg.connect(os.environ["DATABASE_URL"]) as conn, conn.cursor() as cur:
                  cur.execute("SELECT id FROM events_event WHERE slug=%s",(SLUG,))
                  eid=cur.fetchone()[0]
                  cur.execute("""SELECT count(*) FILTER (WHERE preview_key<>''),
                                        count(*) FILTER (WHERE status='pending_review'),
                                        count(*) FILTER (WHERE status='processing'),
                                        count(*)
                                   FROM photos_photo WHERE event_id=%s""",(eid,))
                  return cur.fetchone()

          print("=== AVANCE ===")
          for i in range(6):
              prev, pend, proc, total = foto_estado()
              cola = r.llen("fast")
              print(f"  t+{i*30:3}s  cola fast={cola:<5} con preview={prev:<5} "
                    f"listas para aprobar={pend:<5} procesando={proc}")
              if i < 5: time.sleep(30)

          prev, pend, proc, total = foto_estado()
          print(f"\n=== RESULTADO ===")
          print(f"  total de fotos:        {total}")
          print(f"  con preview generado:  {prev}")
          print(f"  listas para aprobar:   {pend}")
          print(f"  todavia procesando:    {proc}")
          print(f"  tareas en cola 'fast': {r.llen('fast')}")
          if prev > 0:
              print("\n  ARREGLADO: el worker esta generando previews.")
          else:
              print("\n  TODAVIA EN 0: el worker no arranco, revisar sus logs.")
          PYX
